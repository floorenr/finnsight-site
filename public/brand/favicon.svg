<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" role="img" aria-label="Finnsight favicon">
  <!-- Solid background (dark/light browser safe) -->
  <rect width="64" height="64" rx="14" fill="#0F2D5C"/>

  <!-- Lens ring -->
  <!-- Center (28,28), radius 14 -->
  <circle
    cx="28"
    cy="28"
    r="14"
    fill="none"
    stroke="#FFFFFF"
    stroke-width="3"
  />

  <!-- Data bars (neutral pattern) -->
  <rect x="22" y="28" width="3" height="8" rx="1" fill="#FFFFFF"/>
  <rect x="27" y="30" width="3" height="6" rx="1" fill="#FFFFFF"/>
  <rect x="32" y="29" width="3" height="7" rx="1" fill="#FFFFFF"/>

  <!-- Handle (tangent to ring, no overlap) -->
  <!-- Ring outer radius = 14 + 1.5 ≈ 15.5 -->
  <!-- Tangent start ≈ (28 + 11, 28 + 11) -->
  <line
    x1="39"
    y1="39"
    x2="48"
    y2="48"
    stroke="#FFFFFF"
    stroke-width="3.5"
    stroke-linecap="round"
  />
</svg>
